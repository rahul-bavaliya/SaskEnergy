# Tool-Use Disclosure

**Claude (Anthropic)**
- Used for research and development
- Drafting and iterating on the LLM system prompt in `synthesizer.py`

## APIs and Models

**Nvidia NIM API** [Easily Access & Free to use]
- `nvidia/nv-embed-v1` — embedding model 

- `google/gemma-3n-e2b-it` — LLM used for two purposes:
  1. Structured field extraction from the new project brief (JSON output)
  2. Grounded synthesis of the estimating support report
- Production deployment:  Azure OpenAI endpointsto keep data within the enterprise boundary.

---

## Frameworks and Libraries
- Python- 3.11 | Core language
- IDE | Antigravity
- `openai` (SDK) | API client for Nvidia-compatible endpoint
- `python-dotenv` | Secure API key loading from `.env`
- Standard library (`csv`, `json`, `re`, `pathlib`, `math`)
- No external vector database was used.